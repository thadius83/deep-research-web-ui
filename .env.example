# AI Provider Settings
OPENAI_KEY=""                   # Your OpenAI API key (required if not set in UI)
OPENAI_ENDPOINT=""             # Optional: Custom endpoint for OpenAI-compatible APIs (e.g., http://ollama:11434/v1)
OPENAI_MODEL=""                # Model to use (e.g., "gpt-4", "deepseek-r1:32b")
CONTEXT_SIZE="128000"          # Optional: Context size for the model

# Web Search Provider Settings
DEFAULT_SEARCH_PROVIDER="" # Default search provider ("tavily" or "firecrawl")

# Tavily Settings
TAVILY_KEY=""                  # Your Tavily API key (required if using Tavily and not set in UI)
TAVILY_BASE_URL=""            # Optional: Custom endpoint for Tavily API (defaults to https://api.tavily.com)

# Firecrawl Settings
FIRECRAWL_KEY=""              # Your Firecrawl API key (required if using Firecrawl and not set in UI)
FIRECRAWL_BASE_URL=""         # Optional: Custom endpoint for self-hosted Firecrawl (e.g., http://x.x.x.x:3002)
